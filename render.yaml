services:
  - type: web
    name: skylark-bi-agent
    runtime: docker
    dockerfilePath: Dockerfile
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: MONDAY_API_KEY
        sync: false
      - key: PYTHON_VERSION
        value: "3.11"
    autoDeploy: true
